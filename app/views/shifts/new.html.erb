<% if session[:role] == 'manager' %>
<%= render partial: 'partials/manager_nav_bar' %>

<% else %>

<%= render partial: 'partials/admin_nav_bar' %>
<% end %>


<h2 class="heading">New Shift</h2>
<div class="center-align">
    <!--Create a button linking to stores list-->
    <% if session[:role] == 'admin' %>
    <% admin_path=admin_shifts_view_index_path %>

    <a class="waves-effect waves-red deep-orange darken-4 hoverable btn white-text" href=<%=  admin_path  %>>List Shifts</a></li>
    
    <% else %>
        <% manager_path=manager_shifts_view_index_path %>
     <a class="waves-effect waves-red deep-orange darken-4 hoverable btn white-text" href=<%= manager_path %>>List Shifts</a></li>
     <% end %>
</div>
<!--Render the form and link the variable store to the instance variable @store object created by the form-->
<%= render 'form', shift: @shift %>







<% if session[:role] == 'manager' %>
<%= render partial: 'partials/manager_footer' %>

<% else %>

<%= render partial: 'partials/admin_footer' %>
<% end %>

